apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hipster-shop.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hipster-shop.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "hipster-shop.name" . }}
    spec:
      containers:
        - name: frontend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
